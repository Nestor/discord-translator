/*eslint-disable*/
module.exports = function(data)
{
/*
   // --------------------
   // Supported Languages
   // --------------------

   output = "\n";

   text.langList.forEach(function(lang)
   {
      var end = " ,  ";
      if (lang === "Zulu")
      {
         end = ".";
      }
      output += "`" + lang + "`" + end;
   });

   sendBox({
      channel: channel.id,
      color: colorInfo,
      text: text.supported + output
   });
   return;
*/
};
